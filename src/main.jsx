import React from "react";
import ReactDOM from "react-dom/client";
import BitsyBridgeDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BitsyBridgeDashboard />
  </React.StrictMode>
);
